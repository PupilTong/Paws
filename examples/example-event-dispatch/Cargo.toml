[package]
name = "example-event-dispatch"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rust-wasm-binding = { path = "../../rust-wasm-binding" }

[features]
coverage = ["rust-wasm-binding/coverage"]

[workspace]
