[package]
name = "ios-renderer-backend"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
rayon = "1.10"
fnv = "1.0"

[build-dependencies]
cbindgen = "0.26"

[dev-dependencies]
dhat = "0.3.3"
